# Model Weights

Place pretrained or fine-tuned weight files here (e.g., `visual_frontend.pt`, `video-only.pt`, `trained_model.pt`, `language_model.pt`). The code loads paths from `lipreading/config.py`, which points to this folder by default.
